name: articles
description: Package contains Articles logic

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.3
  cloud_firestore: ^2.4.0
  logger: ^1.0.0
  freezed_annotation: ^0.14.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.3
  build_runner: ^2.0.6
  freezed: ^0.14.2
  json_serializable: ^4.1.3

flutter:
  uses-material-design: true
